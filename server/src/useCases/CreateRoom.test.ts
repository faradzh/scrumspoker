// import { describe, it, expect } from "vitest";

// import CreateRoom from "./CreateRoom";
// import { RequestUser } from "../infrastructure/auth/types";

// describe("CreateRoom", () => {
//   it("should create a new room entity and add it to the store", async () => {
//     const roomData = {
//       name: "RE room#1",
//       participants: [],
//       estimationMethod: "fibbonachi" as const,
//     };

//     const roomRepository = new InMemoryRoomRepository();
//     // const createRoom = new CreateRoom(roomRepository);

//     // const newRoom = await createRoom.execute(roomData, {} as RequestUser);

//     // expect(newRoom.name).toEqual(roomData.name);
//   });
// });
