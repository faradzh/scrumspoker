// import JiraIntegration from "../entities/JiraIntegration";

class GetIntegrationIssues {
    constructor() {
        // to-do
    }
    
    // public async execute(data: JiraIntegration) {
    //     const jiraIssueUrlPart = 'issue/SCRUM-1';

    //     const email = "faradj.musaev@gmail.com";
    //     const apiToken = process.env.JIRA_API_TOKEN!;
 
    //     const jiraIntegration = new JiraIntegration(jiraUrl, email, apiToken); 

    //     const headers = { Authorization: jiraIntegration.getAuthorizationHeader(), "Content-Type": "application/json" };
        
    //     const response = await fetch(jiraIntegration.baseUrl, {
    //         headers
    //     });

    //     const data = await response.json();
    //     return data;
    // }
}

export default GetIntegrationIssues;